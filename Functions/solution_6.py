# Problem: Create a lambda function to compute the cube of a number.

# This are the functions which are used rarely or one time in the codebase, or use to run some function on the go

# lamba parameter: working
cube= lambda x: x**3

print(cube(3))
print(cube(5))
print(cube(10))